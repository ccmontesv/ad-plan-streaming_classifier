{
  "name": "ad-plan-classifier",
  "context": "..",
  "dockerFile": "../Dockerfile",
  "workspaceFolder": "/app",
  "mounts": [
    "source=${localWorkspaceFolder}/reports,target=/app/reports,type=bind"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-python.python"
  ]
}
